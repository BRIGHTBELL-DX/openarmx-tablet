# OpenArmX Robot Control Tablet

## 프로젝트 개요
OpenArmX A2 듀얼 암 로봇의 **현장 운영 컨트롤 태블릿 웹앱**.

시뮬레이터(robot_simulator)에서 제작된 YAML 모션 파일 + 음악 파일을 로봇에 로드하여
현장에서 재생/관리하는 운영 UI.

## 시스템 구성

```
[iptime 공유기] ── 유선 ──▶ 로봇 PC
      │                     ├── FastAPI 서버 (포트 8000)
      │                     │    ├── REST API  (파일 관리, 재생 명령)
      │                     │    └── WebSocket (상태 실시간 스트림, E-STOP)
      │                     └── 이 웹앱 서빙 (포트 3000 or 8000/static)
      │
      └── WiFi ──▶ 태블릿
                   └── http://192.168.0.10:8000 접속 (인터넷 불필요)
```

- 로봇 PC가 웹앱 + API 동시 서빙
- 태블릿은 같은 로컬 WiFi로 접속
- 인터넷 없이 완전 폐쇄망 운영 가능
- mDNS: `openarmx.local` 로 IP 없이 접속 가능 (추후)

## 타겟 해상도
**2000 × 1200px** (태블릿 가로 고정 레이아웃)

## 핵심 기능 (구현 대상)

### 1. 파일 관리
- YAML + 음악 파일 페어링 (세트로 관리)
- 시뮬레이터에서 전송받은 파일 목록 표시
- 파일 메타 표시: 길이(초), 포즈 수, 파일명
- 버전 관리 (같은 공연 v1/v2/v3)
- 태그/분류

### 2. 재생 제어
- 원터치 실행 (YAML + 음악 동시)
- 속도 배율: 0.5x ~ 2.0x
- 음악-모션 싱크 오프셋 조정 (±2초)
- 구간 재생 (부분 반복)
- 오디오 출력 선택 (내부/외부)

### 3. 플레이리스트
- 여러 YAML+음악 셋을 순서대로 묶기
- 드래그로 순서 조정
- 셋 간 인터벌 설정 (쿨다운용)
- 대기 포즈 설정 (인터벌 중 취할 자세)
- 루프 여부 설정
- "지금 바로 재생" / "다음 순서에 삽입"
- 현재/다음/그 다음 공연 3단계 미리보기

### 4. 상태 모니터링 (실시간 WebSocket)
- 관절별 온도 (14관절: L1~L7, R1~R7)
- 관절별 토크
- 통신 상태 (연결/끊김)
- 에러 로그
- 누적 구동 시간

### 5. 과열 보호
- 관절 온도 임계값 설정
- 임계값 초과 시 다음 공연 자동 딜레이
- 자동 쿨다운 삽입 (온도 기반)
- 특정 온도 이상이면 해당 셋 스킵
- 오늘 총 구동 시간 표시

### 6. 캘리브레이션
- 홈 포지션 확인 및 복귀
- 워밍업 루틴 (공연 전 저속 전 관절 스트레칭)
- 개별 관절 수동 조작 (슬라이더)
- 레이턴시 캘리브레이션 (음악-모션 딜레이 측정)
- 관절 범위 테스트

### 7. 스케줄링
- 요일/시각별 자동 실행
- 다음 공연까지 카운트다운 표시
- 날짜별 ON/OFF

### 8. 안전
- E-STOP 버튼 (항상 화면에 표시)
- WiFi 끊김 감지 → 자동 비상정지
- 운영자 PIN 잠금
- 공연 전 체크리스트

## 기술 스택

### 프론트엔드 (이 저장소)
- Vanilla HTML/CSS/JavaScript (ES Modules)
- Three.js 불필요 (3D 없음)
- 외부 라이브러리 최소화

### 백엔드 (별도: 로봇 PC에서 실행)
- Python FastAPI
- WebSocket (실시간 상태)
- REST API (파일, 재생 명령)

## API 엔드포인트 (예정)

```
GET  /api/files          → YAML+음악 파일 목록
POST /api/files/upload   → 파일 업로드
POST /api/play           → 재생 시작 {yaml, music, speed}
POST /api/stop           → 정지
POST /api/estop          → 비상 정지
GET  /api/status         → 현재 상태
WS   /ws/status          → 실시간 상태 스트림
GET  /api/playlist       → 플레이리스트 조회
POST /api/playlist       → 플레이리스트 저장
POST /api/schedule       → 스케줄 설정
```

## GitLab 저장소
- 이 프로젝트는 robot_simulator(GitHub)와 완전 분리된 GitLab 저장소
- 프론트엔드 코드만 관리 (백엔드는 로봇 PC 로컬)

## 관련 프로젝트
- 시뮬레이터: `../robot_simulator/` (GitHub Pages 배포)
- 로봇: OpenArmX A2 (듀얼 암, 14관절: L1~L7, R1~R7)
